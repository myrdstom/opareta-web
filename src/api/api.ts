export const convertCurrency = (
    count: number,
    baseCurrency: string,
    compareCurrency: string
) => {
    console.log(
        count,
        'couunt',
        baseCurrency,
        'baseCurrency',
        compareCurrency,
        ' compareCurrencycalled the currency =======?'
    );
    return null;
};
